git clone https://github.com/microsoft/semantic-kernel.git

Install Polyglot Notebooks extension into VS Code

Open main folder in VS Code

Go to dotnet >> notebooks >> config

Copy the OpenAI file to settings.json

Enter your OpenAI key

Then go through each example in dotnet >> notebooks 0- . . . . . 08-
