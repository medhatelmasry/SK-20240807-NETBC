{
    "schema": 1,
    "description": "Find the city name from the given input",
    "execution_settings": {
        "default": {
            "max_tokens": 1000,
            "temperature": 0.0,
            "top_p": 0.0,
            "presence_penalty": 0.0,
            "frequency_penalty": 0.0
        }
    },
    "input_variables": [
        {
            "name": "input",
            "description": "Input text from the user",
            "default": ""
        }
    ]
}
